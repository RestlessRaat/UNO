# Verification record

## Environment

- Windows, CPython 3.14.0; pygame-ce 2.5.8, websockets 16.0, Pillow 12.0.0, pytest 9.1.1, PyInstaller 6.20.0.
- Chromium 152 with Scratch SVG renderer 15.1.1 for SVGs and Scratch VM 15.1.1 for source comparison.

## Completed checks

- Strengthened Devil revision: all 220 project tests passed, followed by the frozen game/help/live-room smoke checks. The 400 held-out two-player rounds used seeds 52001–52200 with seats exchanged. Devil won 329/400 (82.25%), including 169/200 from seat 0 and 160/200 from seat 1; with Normal anchored at 1000, the direct fitted estimate is 1265.41975. The published evidence uses the native 36-card engine and replaces the previous indirect chain. Regression coverage includes public-history privacy, persistent draw inference, exact 7/0 transfer memory, posterior sampling, deliberate three-card draw continuations, strategic opponent simulation, God compatibility, replay reconstruction, and plugin execution. Initial decision time in a fixed local position was about 0.7 seconds; this is an observation, not a latency bound.

- Modular AI/plugin revision: 214 tests passed. The suite covers immutable public DTOs, external manifest discovery and isolation, configuration validation, rejection of private-information claims, illegal-action and soft-timeout fallback, late-result rejection, per-seat local and LAN selection, protocol v2, fixed Normal disconnect takeover, settings migration, v1/v2 replay compatibility, and dependency boundaries. The bundled `example.random-plus` plugin completed a fixed-seed two-player round in 81 actions. The per-seat selector and generated boolean/enum settings screens were rendered and visually inspected. A full four-seat two-client LAN smoke round finished at revision 391 with matching host/guest winner and scores. The rebuilt executable passed game, help and live-room smoke checks from a temporary directory; the game check copied the external example into the isolated user plugin directory and confirmed it completed a decision recorded in the replay.

- Imported-AI calibration update: 200 project tests plus four adapter tests passed. In the attached 36-card engine, unchanged Devil won 209/400 rounds against the frozen imported AI (52.25%; seeds 40001–40200, both seats). The imported AI previously won 324/400 against the unchanged Normal policy (seeds 30001–30200), anchoring it at 1251.02131. Chaining the observed +15.60610 difference yields Devil 1266.62741; paired bootstrap across both independent seed groups gives a 95% interval of 1213.02577–1324.91119. Previous 35-card Devil calibration is retained as superseded evidence. The loader recomputes both links from win counts, rejects broken chains, and preserves Hard.
- The native-engine adapter checks all 168 card faces, legal action translation, hidden-hand/deck-order isolation, and public roulette cards after the roulette ends. An initial incomplete-adapter run was stopped and discarded; only the corrected full 400-round run was published. Strategies, imported weights and search budgets remained fixed.

- God cache optimization: 192 tests passed; the rebuilt game/help/live-room smoke checks passed. the live draw-pile order is cached by game and pile identity, and rebuilt after recycling; independent simulation cursors share immutable orders. Forced single legal actions bypass omniscient snapshots and search. Hand evaluations and immutable card metadata are reused without reducing search depth or node budget. Six identical positions (seeds 401/402, normal-policy actions 0/20/50) selected identical actions before/after optimization; combined measured decision time fell from about 4.53 s to 2.84 s (37%). This is a local timing sample, not a guaranteed speedup. Cache invalidation, branch isolation and forced-draw bypass have regression tests.

- God search upgrade: 189 tests passed; the rebuilt executable passed game/help/live-room smoke checks. iterative deepening retains only completed root iterations; alpha-beta search uses exact-state transpositions and no fixed eight-move cutoff. Forced draws settle before evaluation. Regression scenarios verify all four roulette colours behind wild cards, exact recycled-deck prediction, and a +10 attack that forces mercy beyond the old four-action horizon. Search is bounded to 12000 generated successors and ten strategic plies, not an assertion of perfect play.

- God / 36-card revision: 183 tests passed; the rebuilt executable passed table, help and live loopback room smoke checks. ordinary, penalty, roulette and AI draws eliminate at 36; drawing a 35th card remains legal. God uses exact private hands/discard/deck order and the same shuffle generator in bounded adversarial search. Simulation parity is checked through complete 2/3/4-player rounds; private snapshots are isolated from the live game and ordinary player views. UI tests cover warning confirmation/cancellation and delayed host commands. The warning and four-difficulty layout were visually inspected. God has no Elo; previous bot ratings remain historical references calibrated with the 35-card rule.

- The Devil Windows build passed table, help and live loopback room checks with `tools/smoke_release.py`. The packaged calibration asset matches the source SHA-256, and the frozen archive contains `uno.devil`. Offline and full four-seat lobby screenshots were visually inspected with all three Elo labels. These are headless checks; physical-device limitations below still apply.

- Devil AI revision: 168 tests passed. Tests cover forced finishing chains, simulation/rule-engine equivalence, card conservation and public-information privacy, complete 2/3/4-player games, asynchronous local decisions, and rejection of stale LAN decisions after a new round, revision change or room closure.
- Held-out Devil calibration: seeds 10001–10200, both seats, 400 rounds; 340 wins (85%), with 174/200 first-seat and 166/200 second-seat wins. Normal is anchored at 1000; Devil fits to 1300.14470. This falls below 1400. Hard was independently rechecked at 2927/4000 wins and 1174.27794. The version-2 calibration book preserves both comparisons and source hashes.
- Independent Devil-vs-Hard benchmark: seeds 20001–20050, both seats, 72/100 wins. This matchup is not added to the Normal-anchored rating. Maximum observed Devil decision time was 1517.29 ms under parallel benchmark load; this is not a universal latency bound. Offline and LAN decisions run in background threads.

- Animation speed and AI Elo revision: 138 tests passed. Motion plays at 1.5x, with opening/draw duration, sound boundary, voice duration and LAN pacing regressions. Elo tests cover the logistic scale, finite small-sample estimates, order-independent accumulation, exchanged seats, reproducibility, missing/invalid evidence and host-supplied rating labels. Offline/host/guest rating screens were rendered and visually inspected.
- Elo calibration: seeds 1–2000, two rounds per seed with Hard/Normal swapping seats, 4000 rounds and 572740 legal actions. Hard won 2927 (73.175%); 1484 wins when seated first and 1443 when second, each out of 2000. With Normal anchored at 1000 and a half-win/half-loss prior, Hard is 1174.27794. `assets/ai_elo.json` records evidence and source hashes; `tools/calibrate_elo.py` reproduces it. The rating models two-player rounds, not multiplayer placement or match points.
- Optional Hard AI revision (2026-09-20): 109 tests passed, including 24 AI tests for legacy Normal compatibility, winning sequences, hand swaps/rotation, threat defense, stacking, two-active-player reverse +4, roulette, snapshot privacy, and full legal games. UI tests cover selection, persistence, local turns, rematches and host/guest synchronization; LAN tests cover authority, invalid difficulty, active-round locking and disconnected-seat substitutes. Difficulty controls were rendered and visually inspected in offline setup and a full four-seat lobby.
- Hard-vs-Normal benchmark: one Hard seat against Normal opponents, every seat position, seeds 1–100, separate RNGs per seat. Hard won 141/200 two-player rounds (70.5%), 172/300 three-player rounds (57.33%), and 219/400 four-player rounds (54.75%). Independent seeds 1001–1050: 77/100 (77%), 84/150 (56%), 110/200 (55%). These are measured bot matchups, not human win-rate guarantees. Run `tools/benchmark_ai.py --seeds 100 --output build/ai-benchmark.json`; held-out results use `--start-seed 1001 --seeds 50 --output build/ai-benchmark-heldout.json`.
- Hard AI latency: the 900-round benchmark observed a maximum decision time of 36.42 ms on this machine. A constructed legal 34-card two-player hand containing all 32 skip/reverse/skip-all cards plus two red cards took 26.60 ms on average and 27.37 ms maximum over 30 decisions. These are observations, not a device-independent latency limit.
- The Hard AI Windows build was rebuilt and its table, help and live loopback room passed `tools/smoke_release.py` on 2026-09-20, from a temporary working directory with Python paths removed. The final validation reran outside the sandbox because Windows denied temporary-directory cleanup inside it.
- Animation-end flicker fix: 57 tests passed on 2026-09-17. Regression checks cross the draw-animation end both before rendering and during rendering, verify all 29 hand cards remain present, and check that completed timelines preserve private opponent hands. Each rendered frame now uses a single animation sample. The separately reported whole-window black flash in windowed mode has not been reproduced or confirmed fixed.
- Animation fidelity revision: 52 tests passed on 2026-09-17. Added coverage for round-robin deals, linear hand reflow, real per-card exchanges, hover geometry, scoring, remote-card concealment and colour-selector press/release. The suite simulates every action of a full round through the presentation timeline and verifies that rule state and replay are unchanged.
- The Python 168-card definitions match the exact source `_initial deck` order and multiplicities.
- 75 seeded AI rounds (25 each for 2, 3 and 4 players) complete and replay identically; card conservation is checked after every command.
- Boundary and scenario tests cover inclusive 35-card elimination in ordinary/penalty/roulette/AI paths, stacking restrictions, committed penalties, two-player reverse +4, last special card, swap, rotation, discard-all, recycling and private draw events.
- Real WebSocket tests exercise joining, readiness, duplicate/stale operations, host authority, late join rejection, reconnection, duplicate token connections, heartbeat timeout, room closure and port conflicts. Server-tick scenarios also cover AI completing disconnected colour, roulette-colour and 7-swap choices.
- A separate two-client-process, four-player round completed with identical winner/scores/revision at both clients. The latest run finished at revision 402, player 2 winning 750 points; runs vary with the random host seed.
- Asset decode tests check all 348 imported/generated card/costume images and 66 unique sound files. The empty source SVG is intentionally represented by a 1x1 transparent PNG.
- Layout tests verify every one of 34 overlapping cards has an individually selectable visible strip, and mouse mapping works at four window aspect ratios.
- UI tests click through the menu into a game, draw from the deck, render selection modals, all eight instructions pages and the result screen.
- Original SB3 was executed locally and its intro/lobby/table screenshots and state saved in `build/baseline`. This verified the live table's blank gradient backdrop at brightness -20, 56.7x88.55 card size and right-hand draw pile.
- The Windows executable ran from a temporary working directory with Python paths removed from its environment. Its table, instructions and live loopback WebSocket room rendered successfully with no crash log. `tools/smoke_release.py` writes the report to `build/frozen-verification.json`. These checks use SDL's dummy video/audio drivers, not a physical screen/speakers.

## Scope of visual comparison

The original artwork, font, hand placement, wheel, backdrops and sounds are reused. The Python port has desktop/LAN-specific menu and connection controls. Animation now follows the Scratch procedures at 30 display updates per second; see `ANIMATIONS.md` for the source-by-source mapping.
The disabled caveman/promotion sequence is not played. Page 1 of the instructions is re-typeset for the 35-card rule.
Actual original VM traces cover 6- and 15-frame transitions. `tools/audit_animation.py` compared 663 card/frame samples; the maximum coordinate/angle error was 5.68e-14. This verifies movement geometry, not identical pixels or all device-dependent sound timings. Renderer-generated GIFs cover opening, draw, per-card exchange, mercy and scoring in `build/animation-preview`.

## Hardware checks still required

- Complete a LAN match on two separate physical Windows machines, including their real network/firewall configuration.
- Run the portable build on a separate clean Windows installation without Python.
- Listen to music/voice mixing on real speakers and compare timing by playing the source and Python versions side by side.

These hardware checks cannot be replaced by localhost clients or headless screenshots.
